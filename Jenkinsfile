stage('Build') {
        sh 'echo"building hex file"'
    }
stage('Calibrate') {
        sh 'echo "calibrating HEX file before the test"'
    }
stage('HITLoopTest') {
        sh 'echo "Testing HEX file"'
      }
